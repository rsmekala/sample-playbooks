---
 - hosts: 10.209.18.85 ansible_user=regress ansible_ssh_pass=MaRtInI
   vars:
    motd_welcome: 'Welcome!!!'
   tasks:
   - name: sample motd
     copy:
      content: "{{ motd_welcome }}"
      dest: ./abc
