--- 
- name: Testing Juniper and Ansible
  hosts: 10.221.132.35 ansible_user=regress ansible_ssh_password=MaRtInI
  connection: local
  roles: 
   - Juniper.junos
  tasks: 
   - name: Retrieve Juniper facts
     junos_get_facts: host={{ ansible_ssh_host }}
     register: vmx_info
  
   - name: Print facts
     debug: msg="{{ vmx_info.facts }}"
